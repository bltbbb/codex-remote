{
  "name": "@codex-remote/protocol-mock",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/engine.ts"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@codex-remote/protocol": "workspace:*",
    "ws": "^8.18.2"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1"
  }
}

